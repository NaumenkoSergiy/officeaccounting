table.table
  thead
    tr style='background-color: #F2F1F0;'
      th
        = sort_link @search, :date, t('money.costs.date'), {}, { remote: true, method: :post }
      th
        = t('contract.counterparty')
      th
        = t('money.costs.contract')
      th
        = t('money.costs.bank_account_c')
      th
        = t('money.costs.type_of_document')
      th
        = t('money.costs.article')
      th
        = sort_link @search, :total, t('money.costs.total'), {}, { remote: true, method: :post }
      th
        = sort_link @search, :type_money, t('contract.type'), {}, { remote: true, method: :post }
      th

  tbody
    = render partial: 'money/registers/registers_list'
