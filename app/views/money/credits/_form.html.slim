= form_for [:money, @credit], remote: :true do |f|
  = label_tag 'Назва'
  br
  = f.text_field :name

  br
  = label_tag 'Тип кредита'
  br
  = f.select :credit_type, options_for_select(Credit::CREDIT_TYPE)

  br
  = label_tag 'Номер рахунку'
  br
  = f.text_field :account_number, :class => 'number', maxlength: 8

  br
  = label_tag 'Банк'
  br
  = f.select :bank_id, options_for_select(Bank.select(:name, :id).collect{|b| [b.name, b.id]})
  br
  = f.submit 'Додати'
